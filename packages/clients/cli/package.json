{
  "name": "@agent-os/tui",
  "private": true,
  "version": "0.10.0",
  "description": "Private placeholder workspace for the Agent OS terminal client.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p ../../tsconfig.json --noEmit"
  }
}
