# agents

Agent projects.

Each entry is a self-contained agent project with its own identity, versioning, and project docs, carried here rather than built as part of this product. The shared runtime they build on lives in the product packages.
