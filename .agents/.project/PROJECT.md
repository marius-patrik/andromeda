# Project

`agents-mono` is the root repository for the Agentos topology. The root package exposes the `agents` CLI and validates the `os/agents-manager` TypeScript test surface.

Most child directories are submodules. Avoid recursive submodule mutation for root-only workflow and policy changes.
